import { DragResizeDirective } from './drag-resize.directive';

describe('DragResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new DragResizeDirective();
    expect(directive).toBeTruthy();
  });
});
